---
summary: "Guidance for adding repo-local ontology concepts versus escalating them upstream."
read_when:
  - "You are deciding whether a concept belongs locally or in a shared overlay"
  - "You are adding a new repo-local ontology concept"
type: "reference"
---

# Repo Concepts

Add repo-local concepts here only when needed.

Guideline: if a concept is useful across many repos, propose it in the company overlay (or kernel), not here.
