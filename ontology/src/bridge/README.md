---
summary: "Purpose and scope of the ontology bridge layer in this repo."
read_when:
  - "You are editing ontology-to-code mappings"
  - "You need to understand what belongs in the bridge layer"
type: "reference"
---

# Bridge

The bridge layer links ontology concepts to repo artifacts (code symbols, APIs, schemas).

- `mapping.yaml` maps stable concept IDs → repo-local symbols/paths.
